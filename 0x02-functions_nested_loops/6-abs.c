#include "main.h"
/**
 * _abs - This function computes the absolute value of an integer.
 *
 * @n: Parameter of the function
 *
 * Return: -n if n is negative and n if not
 */

int _abs(int n)
{

	if (n < 0)
	{
		return (-n);
	}
	return (n);
}
